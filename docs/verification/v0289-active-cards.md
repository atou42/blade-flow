# v0.2.89 主动卡牌验收

日期：2026-05-23

目标：确认 4 张手牌不再只是展示。战斗区滑动继续负责方向动作，点按手牌会消耗对应卡牌并触发主动效果。

验收项：

- `node --check src/game.js` 通过。
- 手机视口进入 Boss 实验战后，点按第一张 `格挡`，结果为 `card-active`，手牌被消耗，战斗日志显示主动使用效果。
- 手机视口从战斗区任意位置左划，结果仍为 `screen` 输入，不消耗手牌。
- 点按 `破甲` 会造成 Boss 伤害，确认主动攻击类卡牌有效。
- 主动用牌不会阻断全屏滑动出招，二者输入分工清晰。
- 线上 `games.atou.cc/combo-card-roguelike/versions/a/` 拉到 `v0.2.89` 缓存版本后复验。

结果：

- 本地 `http://127.0.0.1:4174/?bossLab=1` 通过。点按 `格挡` 返回 `card-active`，手牌第 0 格变成空槽；战斗区左划返回 `screen`，手牌不变；点按 `破甲` 后 Boss 血量下降。
- 线上 `https://games.atou.cc/combo-card-roguelike/versions/a/?bossLab=1&t=v0289c` 通过。页面显示 `v0.2.89 主动卡牌`，点按手牌日志显示主动使用，战斗区滑动不改变手牌。
